[defaults]
inventory = inventory
remote_user = jenkins
remote_pass = P@ssw0rd
#private_key_file = jenkins_rsa
become = true
become_method = sudo

#[privilege_escalation]
#become = true
#become_method = sudo
#become_user = root